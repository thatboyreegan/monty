#include "monty.h"

/**
*nop - does nothing.
*@stack: the stack to.
*@line_number: the line number.
*/

void nop(stack_t **stack, unsigned int line_number)
{
	UNUSED(stack);
	UNUSED(line_number);
	return;
}
